spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/iids
    username: iids
    password: iids
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
